import type { E2RObject } from "../models";
export function RelationDetailDialog({ relation, sourceId, targetId, source, target, name, description, onNameChange, onDescriptionChange, onSave, onDelete, onClose }: { relation: E2RObject; sourceId: string; targetId: string; source: E2RObject | null; target: E2RObject | null; name: string; description: string; onNameChange: (value: string) => void; onDescriptionChange: (value: string) => void; onSave: () => void; onDelete: () => void; onClose: () => void }) { const originalName = typeof relation.name === "string" ? relation.name : ""; const originalDescription = typeof relation.description === "string" ? relation.description : ""; return <><button className="detail-backdrop" type="button" aria-label="Close Relation Detail" onClick={onClose} /><aside className="detail" role="dialog" aria-modal="true" aria-labelledby="relation-detail-title"><div className="detail-header"><h3 id="relation-detail-title">Relation Detail</h3><button type="button" onClick={onClose}>Close</button></div><dl><dt>ID</dt><dd>{relation.id}</dd><dt>Source</dt><dd>{typeof source?.name === "string" ? source.name : sourceId}</dd><dt>Target</dt><dd>{typeof target?.name === "string" ? target.name : targetId}</dd></dl><div className="detail-fields"><label htmlFor="relation-name">Name</label><input id="relation-name" type="text" value={name} onChange={(event) => onNameChange(event.target.value)} /><label htmlFor="relation-description">Description</label><textarea id="relation-description" rows={4} value={description} onChange={(event) => onDescriptionChange(event.target.value)} /></div><div className="detail-actions"><button type="button" disabled={name === originalName && description === originalDescription} onClick={onSave}>Save Relation</button></div><div className="detail-danger"><span>Danger zone</span><button type="button" onClick={onDelete}>Delete Relation</button></div></aside></>; }
