import type { CoreObjectDraft, Dataset, DeletionAssessment, DeletionResult, E2RObject, IdCandidateGenerator, RelationCreationResult } from "../models";
import { createCoreObjectId } from "./IdentifierService.ts";
function optionalText(value: string | undefined): string | undefined { return typeof value === "string" && value.trim() ? value : undefined; }
export function createRelation(dataset: Dataset, draft: CoreObjectDraft & { sourceId?: string; targetId?: string }, nextCandidate?: IdCandidateGenerator): RelationCreationResult { const sourceId = typeof draft.sourceId === "string" ? draft.sourceId : ""; const targetId = typeof draft.targetId === "string" ? draft.targetId : ""; if (!sourceId || !targetId) return { dataset, refusal: "relation_endpoint_required" }; if (!dataset.entities.some(({ id }) => id === sourceId)) return { dataset, refusal: "relation_source_entity_required" }; if (!dataset.entities.some(({ id }) => id === targetId)) return { dataset, refusal: "relation_target_entity_required" }; const id = createCoreObjectId(dataset, nextCandidate); const relation: E2RObject = { id, sourceId, targetId }; const name = optionalText(draft.name); const description = optionalText(draft.description); if (name !== undefined) relation.name = name; if (description !== undefined) relation.description = description; return { dataset: { ...dataset, relations: [...dataset.relations, relation] }, relationId: id }; }
export function assessRelationDeletion(dataset: Dataset, relationId: string): DeletionAssessment { return dataset.relations.some(({ id }) => id === relationId) ? { ready: true } : { ready: false, reason: "relation_not_found" }; }
export function deleteRelation(dataset: Dataset, relationId: string): DeletionResult { const assessment = assessRelationDeletion(dataset, relationId); if (!assessment.ready) return { dataset, deleted: false, reason: assessment.reason }; return { dataset: { ...dataset, relations: dataset.relations.filter(({ id }) => id !== relationId) }, deleted: true, deletedId: relationId }; }
export function getRelationDetail(dataset: Dataset, relationId: string) { const relation = dataset.relations.find((candidate) => candidate.id === relationId); if (!relation) return null; const sourceId = typeof relation.sourceId === "string" ? relation.sourceId : ""; const targetId = typeof relation.targetId === "string" ? relation.targetId : ""; const source = dataset.entities.find((entity) => entity.id === sourceId) ?? dataset.events.find((event) => event.id === sourceId) ?? null; const target = dataset.entities.find((entity) => entity.id === targetId) ?? dataset.events.find((event) => event.id === targetId) ?? null; return { relation, sourceId, targetId, source, target }; }
export function updateRelationDetails(dataset: Dataset, relationId: string, updates: { name: string; description: string }): Dataset { return { ...dataset, relations: dataset.relations.map((relation) => { if (relation.id !== relationId) return relation; const updated = { ...relation }; if (updates.name.trim()) updated.name = updates.name; else delete updated.name; if (updates.description.trim()) updated.description = updates.description; else delete updated.description; return updated; }) }; }
