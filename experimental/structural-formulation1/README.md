# Production-native structural formulation 1 (research only)

The pure generator is `tools/structural-formulation.mjs`; it has no imports,
DOM, Node, network, Worker, Product presentation, or Dataset-writing dependency.
The Node runner supplies the existing authoritative Product evaluator.
No browser runtime benchmark or Product provider integration is claimed.

Reproduce from the LiaisonScape repository root:

```text
node tools/structural-formulation-audit.mjs
node tools/structural-formulation-audit.mjs --small
node tools/structural-formulation-audit.mjs --scaling
node tools/structural-formulation-audit.mjs --g3
```

The first command creates `audit.json` for five formulations in two
orientations and the retained references. The second keeps crossing-ring,
ordered-stress, and twin-spokes (six candidates), verifies the same selected
coordinates/digest/metrics as the ten-candidate audit on all 11 cells, and
records generation-only scaling. `small.json` contains the second measured
small-portfolio repetition. Candidate metrics are before finalization;
`selected.metrics` is after the existing round-once boundary.

`scaling.json` measures full evaluation at 20/100 and 24/144 Nodes/Relations.
`generation-scaling.json` is generation-only and must not be confused with
Product/E2E timing. `g3.json` uses `ablation=off` with the G3 anisotropic
transform and Stage-2 bypass. All arms use the same exact geometry/metadata
cache flags. The runner's elapsed time excludes process startup and JSON output.

`provenance.json` records Node version and source/canonical-fixture hashes.
Synthetic inputs are generated by the diagnostic script; `k7-7-minus-one`
removes its first relation to perturb exact structural symmetry. No canonical
Dataset copies or fixture-specific coordinates are stored here.

Disposition: RETUNE. Dense crossings/hits improve, but fit reduces screen
separation; Apollo has a new crossing and Titanic JA has label-nearness drift.
No user Human ACCEPT is claimed. See the E2R-SPEC structural-formulation1 result.
