# Cross-Family Product-Authoritative Auto Layout Portfolio Selector 1

This index records a small Actual Product smoke only. It is not formal
acceptance, user comparison, or Human Review evidence.

## Inspection URLs

All URLs use the disposable, read-only Product preview seam:

- Dense selector result (Post): `http://127.0.0.1:5174/e2r-liaison-scape/experimental/product-evaluation-seam/cross-family-product-authoritative-auto-layout-portfolio-selector1/?fixture=dense-k7-7&candidate=selected`
- Dense Product oracle (free-form): `http://127.0.0.1:5174/e2r-liaison-scape/experimental/product-evaluation-seam/cross-family-product-authoritative-auto-layout-portfolio-selector1/?fixture=dense-k7-7&candidate=oracle`
- Japanese long-label selector result (free-form): `http://127.0.0.1:5174/e2r-liaison-scape/experimental/product-evaluation-seam/cross-family-product-authoritative-auto-layout-portfolio-selector1/?fixture=label-heavy-ja-10&candidate=selected`

## Smoke observations

- `dense-k7-7 / selected`: the selector's Post result is compact enough to
  remain on one Product canvas, but 49 ordinary Relations still produce a
  dense repeated-label and crossing surface that is not release-readable.
- `dense-k7-7 / oracle`: the free-form result changes the geometry and lowers
  the current evaluator's crossing metric, but the wider arrangement is
  clipped at the initial viewport edge and remains visually crowded. The
  machine oracle is therefore not a Human Review candidate.
- `label-heavy-ja-10 / selected`: the selector correctly reaches the
  free-form family by machine metrics, but long Japanese Node/Relation labels
  are spread over a broad canvas and association is still difficult at
  overview scale. Zero counted crossings does not establish readability.

These observations are consistent with the earlier Frontier/Post and
topology-aware free-form inspections. They do not overturn prior human
evidence and do not name a portfolio winner.

## Evidence guard

The selector artifact uses fresh evaluation through the current Product
presentation/quality path. Persisted Frontier/Post/free-form summaries are
lineage inputs for positions and prior fields only. The artifact records 21
persisted-field mismatches rather than silently treating historical metrics as
current authority.
